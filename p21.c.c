#include <stdio.h>

int fibonacci(int num);

int main()
{
    int num,a=0,b=1,c;
    printf("Enter the number term ");
    scanf("%f",&celsius);
    printf("The room temperature in celsius to fahrenheit is : %f",conversion(celsius));

}




float conversion(float celsius)
{
    float convert = (celsius*1.8)
    +32;
    return convert;
}
